#include <stdio.h>

int main(void)
{
  //(1)
  /*
  int A, B, V, temp = 0;
  scanf("%d %d %d", &A, &B, &V);
  while(1)
  {
    temp++;
    V -= A;
    if(V <= 0)
    {
      printf("%d", temp);
      break;
    }
    V += B;
  }
  */

  //(2)
  /*
  int n, sum = 0;
  int ar1[100], ar2[100];
  scanf("%d", &n);
  for(int i = 0; i < n-1; i++)
  {
    scanf("%d", &ar1[i]);
  }
  for(int i = 0; i < n; i++)
  {
    scanf("%d", &ar2[i]);
  }
  sum += ar2[0]*ar1[0];
  for(int i = 1; i < n-1; i++)
  {
    if(ar2[i] < ar2[i+1])
    {
      sum += ar2[i] * (ar1[i] + ar1[i+1]);
      i++;
    }
    else
    {
      sum += ar2[i] * ar1[i];
    }
  }
  printf("%d", sum);
  */

  //(3)
  /*
  int arr1[100], arr2[100], i = 0;
  gets(arr1);
  gets(arr2);
  printf("%s %s", arr1, arr2);
  */
  
  //(4)
  /*
  int n, m;
  scanf("%d", &n);
  scanf("%d", &m);
  for(int i = 0; i < n; i++)
  {
    scanf("%d %d", );
  }
  */
  return 0;
}
